CREATE TABLE pages (
    search INTEGER NOT NULL,
    pageid INTEGER NOT NULL,
    UNIQUE (search, pageid)
);

insert:
INSERT OR IGNORE INTO pages (search, pageid)
VALUES (?, ?);

isSeen:
SELECT EXISTS (
    SELECT 1 FROM pages
    WHERE search = ? AND pageid = ?
);